apiVersion: v1
kind: Service
metadata:
  labels:
    app: financials-tracker
  name: financials-tracker-service
  namespace: financials-tracker
spec:
  type: ClusterIP
  selector:
    app: financials-tracker
  ports:
    - name: targetport
      port: 80
      protocol: TCP
      targetPort: 80
